name: Deploy

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: |
          git -C /root/docker/curse-web fetch origin master
          git -C /root/docker/curse-web reset --hard origin/master

      - name: Rebuild and restart container
        run: docker compose -f /root/docker/curse-web/docker-compose.yml up -d --build
